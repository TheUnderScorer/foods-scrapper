import * as React from 'react';
import { FC } from 'react';

const RegisterForm: FC = () =>
{
    return (
        <div>
            Register component
        </div>
    );
};

export default RegisterForm;
